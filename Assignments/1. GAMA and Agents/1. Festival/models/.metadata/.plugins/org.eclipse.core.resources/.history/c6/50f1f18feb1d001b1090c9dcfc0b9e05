///***
//* Name: Festival
//* Athours: Emil Stahl & Peyman Peirovifar

model Festival

global {
	
	init {
		
		seed <- 10.0;
		bool alternateFlag;
		
		create Guest number: rnd(15,25)	{
			location <- {rnd(100), rnd(100)};
		}
		
		create FoodStore number: rnd(3,6);
		create DrinkStore number: rnd(3,6);
		
		create InformationCenter number: 1 {
			location <- {50, 50};
		}
				
	}

}


species Store {
	
	rgb storeColor <- #white;
	
	
	aspect default {
		draw cube(8) at: location color: storeColor; 
	}
}

species FoodStore parent: Store {
	init {
		storeColor <- #purple;
	}
}

species DrinkStore parent: Store {
	init {
		storeColor <- #green;
	}
}


species Guest skills: [moving] {
	
	int thirsty <- rnd(1000);
	int hungry <- rnd(1000);
	point informationCenterLocation <- {50, 50};
	rgb color <- nil;
	point targetPoint <- nil;
	Store targetStore <- nil;
	bool alternateFlag;
	
	aspect default {
		draw sphere(2) at: location color: color;
	}
	
}

species InformationCenter {

	aspect default {
		draw pyramid(15) at: location color: #black;
	}	
}

	



experiment main type: gui {
	output {
		display map type: opengl 
		{
			species Guest;
			species Store;
			species InformationCenter;
		}
	}
}

